# Assets

This directory contains app assets.

## Structure
- `images/` - App images and backgrounds
- `icons/` - App icons

## Adding Assets

1. Place your assets in the appropriate folder
2. Make sure they are registered in `pubspec.yaml`:

```yaml
flutter:
  assets:
    - assets/images/
    - assets/icons/
```
